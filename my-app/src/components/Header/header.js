import React from "react";
import "./header.css";

const Header = () => (
  <header className = "jumbotron">
    <h1>Cartoon Memory Game</h1>
    <h3>Avoid clicking the same image more than once</h3>
  </header>
);

export default Header;